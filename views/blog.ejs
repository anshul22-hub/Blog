<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Blog</title>
    <style>
        body {
            background: #f8f9fa;
        }
        .blog-title {
            font-weight: 800;
            font-size: 2.7rem;
            margin-bottom: 1.5rem;
            color: #212529;
            letter-spacing: -1px;
        }
        .blog-cover {
            border-radius: 1.25rem;
            box-shadow: 0 6px 32px rgba(0,0,0,0.13);
            max-width: 100%;
            height: 350px;
            object-fit: cover;
            margin-bottom: 2rem;
        }
        .blog-body {
            background: #fff;
            border-radius: 1.25rem;
            padding: 2.2rem 2rem;
            font-size: 1.15rem;
            line-height: 1.8;
            box-shadow: 0 2px 16px rgba(0,0,0,0.08);
            margin-bottom: 2.5rem;
            white-space: pre-wrap;
        }
        .comment-section {
            background: #fff;
            border-radius: 1.25rem;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            padding: 2rem 2rem 1.5rem 2rem;
        }
        .comment-title {
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 1.2rem;
        }
        .input-group input {
            border-radius: 0.5rem 0 0 0.5rem;
        }
        .input-group .btn {
            border-radius: 0 0.5rem 0.5rem 0;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="container mt-5 mb-5">
        <div class="row justify-content-center">
            <div class="col-lg-9">
                <h1 class="blog-title text-center mb-4"> <%= blog.title %> </h1>
                <img src="<%= blog.coverImageURL %>" class="blog-cover d-block mx-auto" alt="Blog Cover" />
                <div class="blog-body mt-3"> <%= blog.body %> </div>
            </div>
        </div>
    </div>
    <%- include('./partials/scripts') %>
</body>
</html>
  